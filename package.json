{
  "name": "dashbot",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Nicholas Sorokin <nicholas.sorokin@hotmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "run": "node dist/main.js",
    "deploy": "yarn build && docker build -t localhost:5000/dashbot . && docker push localhost:5000/dashbot"
  },
  "dependencies": {
    "discord.js": "^11.5.1",
    "haiku-random": "^1.0.0",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^13.5.0",
    "@types/node-fetch": "^2.5.4"
  }
}
